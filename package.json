{
  "name": "markdown-cell-lookup",
  "version": "0.1.0",
  "private": true,
  "description": "Look up a single cell from a markdown table by row key and column name.",
  "type": "module",
  "main": "dist/cli.js",
  "bin": {
    "mdcell": "dist/cli.js"
  },
  "scripts": {
    "build": "tsc",
    "start": "node dist/cli.js",
    "test": "tsc -p tsconfig.test.json && node --test dist-test/test"
  },
  "engines": {
    "node": ">=18"
  }
}
